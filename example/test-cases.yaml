---
images:
  base:
    dockerfile: base/Dockerfile.tpl
    variables:
      alpine:
        - "3.18"
        - "3.19"
        - "3.20"
      multiword:
        - kuku ruku
    labels:
      - base:{{ .tag }}-alpine{{ .alpine }}
